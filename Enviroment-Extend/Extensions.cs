using System;
using UnityEngine;


static class Extensions { 
  /*
  public static Ray ToRay(this Line line) {
    return new Ray(
      new Vector3(
        line.from.x,0,line.from.y
      ),
      new Vector3(
        line.to.x, 0, line.to.y
      )
        );
  }
  */
}